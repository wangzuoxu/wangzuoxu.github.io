<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="{{ description or site.hero.summary }}" />
    <title>{{ title or (site.hero.name + " | 学术主页") }}</title>
    <link rel="stylesheet" href="{{ '/styles.css' | url }}" />
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>
